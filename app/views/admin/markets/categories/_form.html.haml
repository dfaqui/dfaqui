#category-form
  = simple_form_for [:admin, @market.customer, @market, @market.categories.build],
      html: { class: 'ui form' } do |f|

    .fields
      .twelve.wide.field
        = f.input_field :id, collection: @categories, include_blank: false,
          class: 'ui search dropdown'

      .four.wide.field
        = f.button :button, class: 'ui primary button' do
          %i.save.icon
          Salvar

        .ui.red.icon.button{ onclick: 'cancel()', 'data-tooltip': 'Cancelar' }
          %i.remove.icon

:javascript
  $('.ui.dropdown').dropdown();

  function cancel() {
    $('#category-form').remove();
    $('#category-btn').show();
  }
