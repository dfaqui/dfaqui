%h2.ui.header
  = @market.customer.name
  .sub.header Gestão de tipos

.ui.divider

= link_to new_admin_market_category_path(@market.id),
    class: 'ui primary button' do
  %i.plus.icon
  Novo

%table.ui.striped.table
  %thead
    %tr
      %th Nome
      %th Ação
  %tbody
    = render @categories
