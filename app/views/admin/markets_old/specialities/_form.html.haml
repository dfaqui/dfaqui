= simple_form_for([:admin, @market, @category, @market.specialities.build],
    html: { class: 'ui form' }) do |f|

  .required.field
    = f.label :id, 'Especialidade'
    = f.input_field :id, collection: @specialities_dropdown,
      include_blank: false, class: 'ui search dropdown'

  .field
    = f.button :button, class: 'ui primary button' do
      %i.save.icon
      Salvar
