%tr
  %td= speciality.name
  %td
    = link_to admin_market_category_speciality_path(@market.id, @category.id, speciality.id),
        data: { confirm: 'Tem certeza de que deseja remover essa especialidade?' },
        method: :delete, class: 'ui red icon button' do
      %i.remove.icon
