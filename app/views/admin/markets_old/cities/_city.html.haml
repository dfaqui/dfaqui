%tr
  %td= city.name
  %td
    = link_to admin_market_city_path(@market.id, city.id),
        data: { confirm: 'Tem certeza de que deseja remover esse local de entrega?' },
        method: :delete, class: 'ui red icon button' do
      %i.remove.icon
