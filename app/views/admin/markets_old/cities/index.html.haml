%h2.ui.header
  = @market.customer.name
  .sub.header Gerenciar locais de entrega

.ui.divider

= link_to new_admin_market_city_path(@market.id),
    class: 'ui primary button' do
  %i.plus.icon
  Novo

%table.ui.striped.table
  %thead
    %tr
      %th Nome
      %th Ação
  %tbody
    = render @cities
